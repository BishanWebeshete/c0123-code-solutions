/* exported last */
// define a function named last with the parameter of array
// use index to find last element of that array
// return the last index
function last(array) {
  return array[array.length - 1];
}
