/* exported setValue */
// declare a function named setValue with the parameters of object, key, and value
// use bracket notation to update the object key with the inputted value
function setValue(object, key, value) {
  object[key] = value;
  return object;
}
