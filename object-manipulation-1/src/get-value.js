/* exported getValue */
// declare a function named getValues with the parameter of object
// crate an empty array and use a for-in loop to push the value of the property using bracket notation
function getValue(object, key) {
  return object[key];
}
