/* exported getLastChar */
// declare a function named getLastChar with parameter of a string
// in declaration block, use length of string minus one to find last index of string

function getLastChar(string) {
  return string[string.length - 1];
}
