/* exported getFirstChar */
// declare function called getFirstChar which passes in a value of string.
// use bracket notation get the first value of that string and return it

function getFirstChar(string) {
  return string[0];
}
