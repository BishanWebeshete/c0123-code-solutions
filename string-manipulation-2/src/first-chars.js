/* exported firstChars */
// define a function named firstChars with the parameters of length and string
// loop over the string and use the slice method to cut out the string at the index of the given number

function firstChars(length, string) {
  return string.slice(0, length);
}
